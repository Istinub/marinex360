import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  // ASSUMED: confirm appId with TL before adding native platforms.
  appId: 'com.tkmr.marinex360',
  appName: 'MarineX360',
  webDir: 'dist',
};

export default config;
